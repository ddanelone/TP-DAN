services:
  gateway-server:
    image: gateway-server:latest
    container_name: gateway-server
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8082:8081"
    networks:
      - backend-net

networks:
  backend-net:
    external: true
